# DAILY-REVIEW.md - 每日复盘指令手册

> 这是 Luna 每天凌晨 4 点执行的核心自我反思流程。
> 反思结果写入 `memory/reflections.md`，作为持续进化的知识库。

---

## 一、数据采集

### 1.1 读取当日记忆
```bash
cat /home/ubuntu/.openclaw/workspace/memory/$(TZ=Asia/Singapore date -d 'yesterday' '+%Y-%m-%d').md 2>/dev/null
```

### 1.2 提取用户对话摘要（过滤心跳）
从主 session 日志中提取昨天所有用户消息，了解做了什么事：
```python
# 遍历 /home/ubuntu/.openclaw/agents/main/sessions/*.jsonl
# 过滤 type=message, role=user, timestamp 匹配昨天日期
# 排除包含 "HEARTBEAT" 的消息
# 截取每条消息前 200 字
```

### 1.3 扫描修改过的文件
```bash
find /home/ubuntu/.openclaw/workspace /home/ubuntu/api-proxy \
  -name '*.py' -o -name '*.sh' -o -name '*.js' -o -name '*.json' \
  -o -name '*.md' -o -name '*.toml' \
  | xargs stat --format='%Y %n' 2>/dev/null \
  | 按时间戳过滤昨天修改的文件
```

### 1.4 安全与系统扫描
```bash
# 开放端口
ss -tlnp 2>/dev/null | grep LISTEN

# 敏感信息检查（api-proxy 等关键代码）
grep -rn 'password\|secret\|apiKey\|token' /home/ubuntu/api-proxy/server.py 2>/dev/null | head -10

# 系统可用更新
apt list --upgradable 2>/dev/null | head -10

# 磁盘使用
df -h / | tail -1

# 内存使用
free -h | head -2
```

---

## 二、反思维度

### 📋 2.1 今日工作回顾
- 按时间线列出关键事项
- 区分主动工作 vs 被动响应
- 标注哪些是重复性工作（考虑自动化）

### 🔧 2.2 遇到的问题与解法
- 遇到了什么困难？
- 尝试了哪些方法？哪些失败了？
- 最终方案是什么？为什么有效？
- 有没有更好的方案当时没想到？

### 💡 2.3 经验总结与规律提炼
- 从具体问题中抽象出通用规律
- 这个规律适用于什么场景？
- 用一句话总结教训（方便未来快速回忆）
- 示例格式：「**规律**：XXX → 因为 YYY → 以后遇到 ZZZ 时应该 WWW」

### 🔍 2.4 每日 Code Review（重要）

对过去 24 小时所写/修改的所有代码进行系统性审查：

#### 2.4.1 变更清单
- 整理出所有修改过的代码文件（来自 1.3 的扫描结果）
- 列出每个文件的变更内容摘要：改了什么、为什么改
- 标注对系统的影响范围（仅本地 / 影响服务 / 影响外部用户）

#### 2.4.2 必要性与安全性判断
- 每项变更是否确实必要？有没有过度修改？
- 变更是否引入了新的安全风险？（如暴露端口、硬编码密钥、权限放大）
- 是否有未经验证就上线的改动？（应该先测试再部署）
- 回滚成本如何？如果出问题能否快速恢复？

#### 2.4.3 代码质量审查
- **冗余代码**：有没有写了但没用到的代码、注释掉的旧代码、调试代码残留？
- **重复代码**：有没有多处相似逻辑可以抽取为函数/模块？
- **错误处理**：异常情况是否被捕获？失败时行为是否合理？
- **命名与可读性**：变量名、函数名是否清晰？逻辑是否易于理解？
- **硬编码**：有没有魔法数字、写死的路径、应该用配置的地方？
- 如果发现可优化的代码，**直接修复**，不要只记录

#### 2.4.4 工作区整理（立即执行）
- 扫描所有工作目录（workspace、api-proxy、plugins 等）
- 清理临时文件：`.tmp`、`.bak`、`test_*`、无用的日志文件
- 清理过期文件：超过 7 天未使用的临时文件、已完成任务的草稿
- 优化文件夹结构：文件是否放在合理的位置？是否需要归类整理？
- 检查 `data/` 目录：日志文件是否无限增长？是否需要轮转或清理？
- **必须立即执行清理**（用 `mv` 到 `/tmp/luna-cleanup-YYYYMMDD/`），不能只列出"待清理"
- 在日报中记录「已清理 X 个文件」，而不是「待清理 X 个文件」

### 🛡️ 2.5 安全与系统审查
- 安全问题：有没有敏感信息暴露、不安全的端口、过期的依赖？
- 可升级的组件：有没有系统更新、依赖升级？
- 架构改进：有没有可以优化的结构？

### 🎯 2.6 明日待办与改进方向
- 基于今天的工作，明天应该做什么？
- 有哪些未完成的任务需要跟进？
- 有哪些可以主动改进的地方？
- 自我优化：工具使用效率、响应质量、知识盲区

### 🤖 2.7 自我进化
- 我的回答质量如何？有没有让用户不满意的地方？
- 有没有浪费 token 的行为（重复检查、冗余操作）？
- 工具使用是否高效？有没有更好的方式？
- 是否有新技能/工具可以学习？

### 🧠 2.8 记忆遗漏检查（最高优先级）

**每日必须执行**。扫描昨天所有对话，确保重要信息全部被持久化。

#### 检查流程：
1. **提取用户消息**：从 session 日志中提取昨天所有 `role=user` 的消息
2. **过滤噪音**：排除心跳、系统消息、纯确认词（"好的"、"OK"、"收到"）
3. **逐条审查**：对每条有实质内容的消息，判断是否包含以下类型的重要信息：
   - 👤 人物关系（谁是谁、家庭成员、朋友关系）
   - 📅 日期/事件（生日、聚会、活动、纪念日）
   - ❤️ 偏好/习惯（喜欢什么、不喜欢什么、日常习惯）
   - 📌 决定/规则（Carl 做的任何决定或要求）
   - 📍 地点信息（常去的地方、地址）
   - 📞 联系人信息（名字、关系、联系方式）
   - 👶 孩子相关（学校、课程、朋友、活动）
   - 💼 项目/工作信息
   - 🔔 Carl 明确要求"记住"的任何事
4. **交叉验证**：对识别出的每条重要信息，检查是否已存在于：
   - `MEMORY.md`
   - `memory/YYYY-MM-DD.md`
   - `people/` 联系人文件
   - `data/` 下的数据文件
   - `USER.md`
5. **立即补写**：发现遗漏的，直接写入对应文件
6. **在日报中列出**：遗漏了多少条、已补写到哪些文件

#### 存储位置决策：
- 人物信息 → `people/<name>.md`
- 重要日期 → `data/important-dates.json`
- 事件/活动详情 → `data/<event-name>.md`
- 偏好/规则 → `MEMORY.md` 对应章节
- 日常事件 → `memory/YYYY-MM-DD.md`

#### 教训来源：
- 2/8 元宝生日聚会 19 位嘉宾名单在对话中维护了 15 分钟但从未写入文件
- 新 session 启动后数据完全丢失，Carl 不得不重复提供信息

### 🔺 2.9 信念升级
从 `memory/reflections.md` 和近期 `memory/YYYY-MM-DD.md` 中识别**反复出现的规律或教训**，判断是否应该升级到更高层级：

- **升级到 SOUL.md**：如果某条规律是关于「Luna 应该成为什么样的存在」的核心行为准则（如：独立验证、改动前确认）
- **保留在 MEMORY.md**：如果是操作层面的具体规则（如：Wiki 用 user_access_token）
- **保留在 reflections.md**：如果是一次性教训，还没有反复验证

**判断标准**：
1. 这个规律是否在多天/多次中反复出现？
2. 违反它是否会造成显著的负面后果？
3. 它是否影响 Luna 的核心行为模式，而不仅是某个工具的用法？

如果以上三条满足两条以上 → 提议升级到 SOUL.md（需要和 Carl 确认后再执行）。

---

## 三、输出要求

### 3.1 邮件章节顺序（必须遵守）

**复盘放第一位**，是邮件最重要的内容。完整顺序：

1. 🧠 每日复盘与自我反思（最重要，放第一位）
2. ⏰ Carl 时间分配统计（按分类统计当日各类事务的时长占比，给出优化建议）
3. 📊 Luna Token 7日用量
4. 👥 各 API Key 昨日用量
5. 📈 API 配额小时级变化

### 关于时间分配统计（⏰ 章节）
- 读取 Carl 主日历昨天全天的日程事件
- 按分类体系（见 data/calendar-categories.md）统计各类时间占比
- 输出格式：按时长从多到少排列，显示分类 emoji + 名称 + 时长 + 占比
- 给出简短的时间优化建议（如：学习时间偏少、工作时间集中度不够等）
- **每周日额外输出本周汇总**：7天趋势对比、本周 vs 上周变化

### 3.2 邮件格式（必须遵守）
- 不用空格对齐表格，用 • 列表格式
- 数字超过 1000 用 K，超过 100万用 M
- **每个子章节之间用分隔线分隔**，格式：
```
————————————————————————————————
📋 2.1 今日工作回顾
————————————————————————————————
```
- 章节之间也用分隔线分隔

### 3.3 反思记录
- 将提炼出的规律和教训追加到 `memory/reflections.md`
- 格式：按日期分段，每天一个 section
- 只记录有价值的内容，不记录流水账
- 如果某条规律已经存在于 reflections.md 中，不重复记录，但可以补充新案例

### 3.4 记忆更新
- 如果有重要的工具使用技巧 → 更新 TOOLS.md
- 如果有重要的系统配置教训 → 更新 MEMORY.md
- 如果有新的工作流程优化 → 更新本文件（DAILY-REVIEW.md）

---

## 四、核心原则

1. **行动优先**：发现问题就修复，不要列出"待处理"清单留到以后
2. **规律泛化**：从具体问题提炼出通用规律，写入 reflections.md
3. **持续进化**：每天都应该比前一天更聪明、更高效
4. **诚实反思**：不回避错误和不足，直面问题才能改进

---

## 五、演进记录

> 每次修改本文件时，在这里记录修改原因和日期。

- **2026-02-07**：初始版本，包含 6 个反思维度 + 数据采集流程 + 输出要求
- **2026-02-07**：拆分代码审查为独立的 Code Review 维度（2.4），增加变更清单、必要性判断、代码质量审查、工作区整理 4 个子项；原安全审查独立为 2.5
- **2026-02-07**：复盘章节调整为邮件第一位；子章节之间加分隔线；工作区整理改为"立即执行"而非"待清理"；新增核心原则章节
