# 2026-02-12 Daily Log

## 🔧 任务板群 session 撑爆修复（07:00-07:15 SGT）

### 问题
- Luna 任务板群（`oc_630995d9b870d2ff6ab3fa34a4e7315a`）持续报错：`LLM request rejected: invalid_request_error: INVALID_ARGUMENT`
- session context 达到 188k/200k (94%)，API 拒绝处理
- t013、t014 子任务 spawn 后 totalTokens=0，从未实际启动
- 同时存在 session lock 文件卡死问题

### 排查过程
1. 检查 session_status → 发现任务板群 context 94%
2. API 直接测试（curl）→ 正常，排除 API proxy 问题
3. 清空 transcript jsonl 文件 → 无效，内存中旧状态回写了文件
4. 发现 `sessions.json` 中 `systemSent: true` + `totalTokens` 保持旧值
5. 发现卡死的 `.lock` 文件

### 修复步骤
1. 标记 t013/t014 为 failed
2. 清空 transcript 文件（备份为 .bak）
3. 删除卡死的 lock 文件
4. 重置 `sessions.json` 中的 `systemSent=false` + `totalTokens=0`
5. 重启 gateway 清理内存状态

### ⚠️ 经验教训

**Session 清零两步走（无需重启）**：
1. **清磁盘** — 清空/truncate `.jsonl` transcript 文件
2. **清状态** — 重置 `sessions.json` 中的 `systemSent=false`、`totalTokens=0`

**不需要重启 gateway！** OpenClaw 从 `sessions.json` 重新加载状态，清完直接生效。之前误以为需要重启，实际上是 API proxy 的 thinking 参数问题（独立 bug）被错误归因到了 session 内存状态。

**Lock 文件可能残留** — session 异常中断时 `.lock` 文件可能不会被清理，导致后续请求超时报 `session file locked`。手动删除即可。

**子任务 0 tokens = 从未启动** — 如果 spawn 的子任务 `totalTokens=0`，说明它在初始化时就失败了（通常是 API 错误），应立即标记 failed 而不是等超时。

**INVALID_ARGUMENT ≠ context 太大** — 这个错误也可能出现在 context 正常但内容有问题的情况下。排查时先用 curl 直接测 API 确认 proxy 是否正常。

## 🔧 群聊识别脚本（07:30 SGT）

### 问题
- Carl 说「清一下机器人主对话群」，但 session key 是 chat_id 格式，不知道对应哪个群
- 之前每次都靠猜或者列出来让 Carl 选

### 解决
- 写了 `scripts/lark-chat-names.py`：
  - 通过 Lark API `GET /im/v1/chats/{chat_id}` 查群名
  - 支持 `--resolve "群名"` 反查 chat_id
  - 缓存到 `data/chat-names.json`，1 小时 TTL
  - 域名是 `open.feishu.cn`（不是 larksuite！larksuite 返回 404）

### 群聊映射（当前）
| 群名 | chat_id |
|------|---------|
| Luna 卢娜 - 数字员工 | `oc_a2a70c6b4a29c2f2eb6c2500ea42a500` |
| Luna任务 | `oc_0900e63860f8b6d1b08285262701817f` |
| 📋 Luna任务板 | `oc_630995d9b870d2ff6ab3fa34a4e7315a` |
| Luan测试群聊 | `oc_4fe2e6e2dbfd0e6fc35c9dab672ab820` |
| Luna日程 | `oc_7f3ebd31a5cf2fec9170952b29eb2700` |
| Luna机器人主对话 | `oc_680d9c843e6a0ad501de9299a97f3a7e` |
