# 2026-02-08 日志

## TODO
- [ ] 用 AI 来玩小丑牌（Balatro）
- [ ] 给 Luna Lark 应用添加权限（在 Lark 开放平台 → 应用 → 权限管理 → 添加后重新发布）：
  - [ ] `task:task:read` — 读取任务（让 Luna 能用 Lark 原生任务管理 TODO）
  - [ ] `task:task:write` — 创建/修改任务
  - [ ] `task:tasklist:read` — 读取任务列表
  - [ ] `task:tasklist:write` — 创建/修改任务列表
  - [ ] `contact:contact:readonly` — 读取通讯录（让 Luna 能自己查组织里的人）
  - 添加后需要重新发布应用版本，然后 Carl 重新点一次 OAuth 授权链接刷新 scope

## 重要决策
- Carl 明确：**Luna 是协作者，不是指令执行器**。主动思考、研究、推进，不要等指令
- 建立双模式工作机制 → 后来讨论发现繁忙/空闲是伪命题（API 并行，isolated session 不阻塞对话）
- 心跳从 30m 改为 5m，评论检查和定期检查改回 5 分钟
- 后台研究：去掉空闲检测、去掉深夜限制、做完一个立刻做下一个
- 核心原则：**不纠结成本，专注变强变独立**

### 18:40-18:50 定时任务全链路测试

**发现并修复了关键问题：**
1. 子任务（sessions_spawn）无法使用 `message` 工具发飞书消息（报 `Feishu app ID/secret not configured`）
2. 原因：子任务环境不继承主 session 的 Feishu channel 配置
3. 解决方案：创建 `scripts/lark-send-message.sh` 脚本，子任务通过 curl 直接调 Lark Bot API
4. 更新了 HEARTBEAT.md 的子任务消息发送方式文档
5. 更新了 MEMORY.md 记录此限制

**全链路测试 v2 结果（4/4 通过）：**
- ✅ 基础能力（Python、文件读取）
- ✅ 飞书消息（通过脚本发送）
- ✅ Wiki 读取（user_access_token + Lark API）
- ✅ 文件写入

**已部署的自动化：**
- 系统 crontab: 锁文件清理（每5分钟）
- HEARTBEAT.md: 修正子任务消息发送方式


### 22:20 — Fallback 不触发的根因修复
- **问题**：API 代理返回 `"You have exhausted your capacity on claude-opus-4-6-thinking"` (HTTP 400)，OpenClaw 不识别为可 fallback 的错误
- **根因**：`ERROR_PATTERNS.rateLimit` 里有 `"resource has been exhausted"` 但没有 `"exhausted your capacity"` — 措辞不同
- **修复**：在 3 个 `pi-embedded-helpers-*.js` 文件的 `rateLimit` pattern 里加入 `"exhausted your capacity"`
- **备份**：`.bak` 文件在同目录
- **注意**：OpenClaw 更新后需要重新 patch

### 23:20 — Carl 明确核心沟通原则
- **"说一遍就够了"** — Carl 给出的任何决定/需求/规则，必须立刻固化到系统（文件/脚本/配置），不能靠记忆
- 已写入 SOUL.md + MEMORY.md 最高优先级位置
- 触发原因：token-hourly-stats.py 的配额记录功能之前已经讨论过但没有落实，Carl 不得不重复
- 教训：每次收到指令后，立刻执行 + 持久化，不要只是"理解了"就结束

### 23:22 — 配额追踪只关注实际使用的模型
- Carl 只关心 Claude 4.6 配额，不关心 4.5
- TRACKED_MODELS 精简为：claude-opus-4-6-thinking + gemini-3-pro-high（fallback）
- 已更新 token-hourly-stats.py
